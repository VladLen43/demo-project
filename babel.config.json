//  вынес для того чтобы потестить с jest

{
  "presets": [
    ["@babel/preset-env"],
    "@babel/preset-typescript",
    [
      "@babel/preset-react",
      {
        "runtime": "automatic"
      }
    ]
  ]
}
